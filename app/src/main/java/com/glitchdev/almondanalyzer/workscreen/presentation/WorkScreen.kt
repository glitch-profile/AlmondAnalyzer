package com.glitchdev.almondanalyzer.workscreen.presentation

import androidx.compose.runtime.Composable

@Composable
fun WorkScreen() {

    // UI для всего экрана должен быть тут. И написан при помощи Jetpack Compose. Примеры других экранов - UploadScreen.kt и FieldsScreen.kt
    // Отдельные компоненты, специфичные только для этого экрана можешь помещать в папку с этим же экраном, как в uploadscreen.presentation
    // Общие компоненты со стилем приложения уже реализованы в ui.components

}